spring:
  batch:
    job:
      enabled: false
  shell:
    interactive:
      enabled: false

  datasource:
    url: jdbc:h2:mem:testdb
    driverClassName: org.h2.Driver

  data:
    mongodb:
      uri: mongodb://localhost
      port: 0
      database: test_library